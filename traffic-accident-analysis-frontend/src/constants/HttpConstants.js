export const HeaderNameConstants = Object.freeze({
  XSRF_TOKEN: "xsrf_token",
  CONTENT_TYPE: "Content-Type",
});

export const HeaderValueConstants = Object.freeze({
  APPLICATION_JSON: "application/json",
});
